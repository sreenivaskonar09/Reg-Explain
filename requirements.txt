# Replace your current lines with these pinned versions
streamlit>=1.35.0
pandas>=2.0.0
numpy<2.0.0      # CRITICAL: SHAP breaks on Numpy 2.0
xgboost==2.0.3   # Specific version with stable wheels
shap==0.44.1     # Avoids some of the newer build issues
scikit-learn>=1.3.0
plotly>=5.18.0
python-dotenv
google-generativeai
